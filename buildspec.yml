version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
      - echo CODEBUILD_SRC_DIR=$CODEBUILD_SRC_DIR
  build:
    commands:
      - echo Build started on `date`
      - ./gradlew bootJar
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/okozukaiapp-1.0.jar
